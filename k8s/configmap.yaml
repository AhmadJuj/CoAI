apiVersion: v1
kind: ConfigMap
metadata:
  name: dochub-config
  namespace: dochub
data:
  # Backend configuration
  PORT: "5000"
  NODE_ENV: "production"
  
  # Frontend configuration
  # Update this after deploying backend service
  VITE_API_URL: "http://dochub-backend:5000"
